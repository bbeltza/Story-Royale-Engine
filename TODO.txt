- Be able to create tweens on datatypes that will have the Lerp() function (for now it only supports for those with the used operators)

- Animations/Keyframes

- Audio interpolation (specially the gaussian one that the SNES uses to give a more retro-like style to the game)

- Camera effects: They will affect the camera position but shouldn't modify the camera base position (like shakes...)

- Better error handling
	- Errors should not crash, instead they should just inform the user that something went wrong. In case the error won't let the program continue, then crash

- Better logging supports
- Like having a log file opened

- Icon embedder (for the cmake configuration)
	- a python script that takes a or multiple images to make an .ico file (this is easily possible thanks to a library called Pillow)
	- create a resource file (.rc for windows) to embed the generated .ico file to the application (for linux you just have to set the icon at runtime if I'm not missinformed so add that too)

- Work on the game ^^

(current version: 0.011.01 (experimental))

Less likely ideas:

	- Change the pipeline:
		- Make the update and pupdate pipelines run on a separate thread and only run the input processing and render pipelines on the main one