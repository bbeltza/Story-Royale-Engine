#ifndef SRE_ASSERT_H
#define SRE_ASSERT_H

#error Assertion helper for Story Royale's game engine! Work in progress soon. Not available right now

#define SRE_ASSERT(x)
#define SRE_ASSERT_ASSUME(x) // This one will use this C/C++ feature called "assume" for release builds, if available. This gives the compiler more room to optimize by assumning a condition is true

#endif